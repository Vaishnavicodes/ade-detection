# Copy this file to .env and fill in values. Never commit .env.

# MLflow tracking backend.
# Local default — switch to DagsHub or a remote server for shared tracking:
#   DagsHub:  https://dagshub.com/<user>/ade-detection.mlflow
#   Colab:    set in the notebook before importing mlflow_config
MLFLOW_TRACKING_URI=./mlruns
